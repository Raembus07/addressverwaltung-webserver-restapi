<?xml version="1.0"?><!DOCTYPE Configure PUBLIC "-//Jetty//Configure//EN" "http://www.eclipse.org/jetty/configure_9_3.dtd">

<!-- ============================================================= -->
<!-- SSL ContextFactory configuration                              -->
<!-- ============================================================= -->

<Configure id="sslContextFactory" class="org.eclipse.jetty.util.ssl.SslContextFactory$Server">
    <Set name="KeyStorePath"><Property name="jetty.base" default="." />/src/main/resources/keystore.jks</Set>
    <Set name="KeyStorePassword">elielieli</Set>
    <Set name="TrustStorePath"><Property name="jetty.base" default="." />/src/main/resources/keystore.jks</Set>
    <Set name="TrustStorePassword">elielieli</Set>
    <Set name="KeyManagerPassword">elielieli</Set>
</Configure>